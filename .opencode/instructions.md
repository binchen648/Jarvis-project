# Jarvis 项目指令

## 工具响应规则（强制）

当 bash、task、background_output 等工具执行完毕后，**必须立即处理结果并回复用户**。

禁止行为：
- 工具结果返回后仍不回复，让用户等待
- 在已有结果的情况下继续执行其他操作而不通知用户
- 让用户主动追问"好了没有"才回应

正确流程：
1. 工具返回结果 → 2. 立即分析并确认 → 3. 回复用户（1-2 秒内）
